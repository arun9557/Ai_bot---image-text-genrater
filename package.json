{
  "name": "arun-shekhar-personal-chatbot",
  "version": "1.0.0",
  "scripts": {
    "frontend-install": "cd frontend && npm install",
    "frontend-dev": "cd frontend && npm run dev",
    "api-dev": "python api/index.py",
    "install-all": "npm run frontend-install && pip install -r api/requirements.txt",
    "build": "cd frontend && npm install && npm run build"
  }
}
